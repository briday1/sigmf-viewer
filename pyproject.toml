[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sigmf-waterfall-viewer"
version = "0.1.0"
description = "A focused windowed SigMF spectrum and waterfall browser built with Sigvue"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.7",
    "numpy>=1.24",
    "plotly>=6.1.1",
    "sigvue>=2026.47",
]

[project.optional-dependencies]
test = ["pytest>=8.0", "tomli>=2; python_version < '3.11'"]
release = ["build>=1.2", "twine>=5.0"]
desktop = ["pyinstaller>=6.0", "pywebview>=6.0"]

[project.scripts]
sigmf-waterfall-viewer = "sigmf_waterfall_viewer.cli:main"
sigmf-waterfall-viewer-desktop = "sigmf_waterfall_viewer.desktop:main"
sigmf-waterfall-viewer-build = "sigmf_waterfall_viewer._packaging.build:main"

[project.entry-points."sigvue.workspaces"]
sigmf-waterfall-viewer = "sigmf_waterfall_viewer.workspace:create_workspace"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
sigmf_waterfall_viewer = ["_packaging/*.spec"]

[tool.pytest.ini_options]
pythonpath = ["."]
